#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n,x,y,flag=0; cin>>n>>x>>y;
	int arr[n];
	for(int i=0;i<n;i++)
		cin>>arr[i];
	for(int i=0;i<n;i++)
	{
		if((arr[i]<=y)&&(arr[i]>=x))
			flag=1;
		else
			flag=0;
	}
	if(flag==1)
		cout<<"YES";
	else
		cout<<"NO";
}
