#include<bits/stdc++.h>
using namespace std;

int main()
{
    int i,n,t;    cin>>t;
    for(int j=0;j<t;j++)
    {
        string s;    cin>>n;
        set<string> s1;
        for(i=0;i<n;i++)
        {
            cin>>s; s1.insert(s);
        }
        set<string>::iterator it;
        for(it=s1.begin();it!=s1.end();it++)
        {
            cout<<*it<<endl;
        }
    }
return 0;
}
