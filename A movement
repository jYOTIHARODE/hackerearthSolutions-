#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n,sum=0,count;
	cin>>n;
	//int res=n%10;
	for(int i=1;i<=n;i++)
	{
		sum=sum+5;
		count++;
		if(sum>=n)
		{
			break;
		}
	}
	cout<<count;
	//cout<<res;
}
